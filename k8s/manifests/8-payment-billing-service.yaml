apiVersion: apps/v1
kind: Deployment
metadata:
  name: payment-billing-service
  labels:
    app: payment-billing-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: payment-billing-service
  template:
    metadata:
      labels:
        app: payment-billing-service
    spec:
      containers:
      - name: payment-billing-service
        image: 623653227185.dkr.ecr.us-west-1.amazonaws.com/kayak/payment-billing-service:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8005
          name: http
        env:
        - name: PORT
          value: "8005"
        envFrom:
        - configMapRef:
            name: kayak-config
        - secretRef:
            name: kayak-secrets
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "200m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8005
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8005
          initialDelaySeconds: 10
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: payment-billing-service
spec:
  selector:
    app: payment-billing-service
  ports:
  - protocol: TCP
    port: 8005
    targetPort: 8005
  type: ClusterIP
