# Kayak Environment Variables
# Copy this file to .env and fill in your actual values
# NEVER commit .env to git!

# Google Gemini API Key
GEMINI_API_KEY=your_gemini_api_key_here

# To set in Kubernetes production:
# kubectl patch secret kayak-secrets --type='json' -p='[{"op": "add", "path": "/data/GEMINI_API_KEY", "value": "'$(echo -n "YOUR_ACTUAL_KEY" | base64)'"}]'

# Other sensitive keys (already in secrets.yaml but should be moved):
# STRIPE_SECRET_KEY=your_stripe_secret_key
# JWT_SECRET=your_jwt_secret
# SESSION_SECRET=your_session_secret
